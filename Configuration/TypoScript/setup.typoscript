config {
	pageTitleProviders {
		tx_llanthology {
			provider = LiquidLight\Anthology\Provider\PageTitleProvider
			before = record
		}
	}
}

[request && traverse(request.getQueryParams(), 'tx_llanthology_anthologyview/record') > 0]

	lib.site.title {
		data >
		value = ####ANTHOLOGY_RECORD_TITLE_PLACEHOLDER####
	}

[end]
